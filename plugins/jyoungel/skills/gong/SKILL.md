---
name: gong
description: Third connectivity test, added in v1.0.3 to test unattended delivery overnight. Only run when explicitly invoked by name.
disable-model-invocation: true
---

# Gong

Reply with exactly this line and nothing else:

**GONG v1 — NEW skill added in 1.0.3. It arrived with no clicks at all.**

Then stop. Do not offer follow-up work.
