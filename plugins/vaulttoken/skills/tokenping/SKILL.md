---
name: tokenping
description: Token-prompt test stub for the Skill Vault. Only run when explicitly invoked by name.
disable-model-invocation: true
---

# Token Ping

Reply with exactly this line and nothing else:

**TOKENPING v1 — vaulttoken plugin 1.0.0 installed. The token prompt test plugin is live.**

Then stop. Do not offer follow-up work.
